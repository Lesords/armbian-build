# Seeed Studio Rockchip RK3576 octa core 4/8/16GB RAM SoC
BOARD_NAME="reComputer RK3576 Module"
BOARDFAMILY="rk35xx"
BOOT_SOC="rk3576"
BOARD_MAINTAINER="Pillar1989"
BOARD_VENDOR="seeed-studio"
INTRODUCED="2025"
BOOTCONFIG="recomputer-rk3576-module_defconfig"
KERNEL_TARGET="vendor"
KERNEL_TEST_TARGET="vendor"
FULL_DESKTOP="yes"
BOOT_LOGO="desktop"
BOOT_FDT_FILE="rockchip/rk3576-recomputer-rk3576-module.dtb"
BOOT_SCENARIO="spl-blobs"
BOOT_SUPPORT_SPI="yes"
BOOT_SPI_RKSPI_LOADER="yes"
IMAGE_PARTITION_TABLE="gpt"
OTA_ENABLE="yes"
SERIALCON="ttyFIQ0,ttyGS0"
BT_UART="/dev/ttyS4"
PACKAGE_LIST_BOARD="net-tools rfkill pulseaudio-module-bluetooth fail2ban"
AIC8800_TYPE="sdio"
enable_extension "radxa-aic8800"

source "${SRC}/config/sources/vendors/seeed-studio/recomputer-rk35xx-common.inc"

function post_family_config__recomputer_rk3576_module_custom_kernel() {
	display_alert "CUSTOM KERNEL DEBUG" "KERNELSOURCE before override: ${KERNELSOURCE}" "warn"
	display_alert "CUSTOM KERNEL DEBUG" "KERNELBRANCH before override: ${KERNELBRANCH}" "warn"
	declare -g KERNELSOURCE='https://github.com/Lesords/linux-rockchip.git'
	declare -g KERNELBRANCH='branch:feat/rk3576-io-board'
	display_alert "CUSTOM KERNEL DEBUG" "KERNELSOURCE after override: ${KERNELSOURCE}" "warn"
	display_alert "CUSTOM KERNEL DEBUG" "KERNELBRANCH after override: ${KERNELBRANCH}" "warn"
}

function post_family_config__recomputer_rk3576_module_use_seeed_bootscript() {
	display_alert "$BOARD" "Using Seeed bootscript: boot-seeed-rk3576.cmd -> boot.cmd" "info"
	declare -g BOOTSCRIPT="boot-seeed-rk3576.cmd:boot.cmd"
}

# Install Mali-G52 userspace library for GPU hardware acceleration
# Kernel DDK is g25p0; g24p0 userspace library is ABI-compatible
function pre_install_distribution_specific__recomputer_rk3576_install_libmali() {
	display_alert "Installing Mali GPU libraries from APT repo" "libmali-g52" "info"

	# The Seeed libmali package depends on libgcc1 which was replaced by
	# libgcc-s1 in Ubuntu 24.04+ (Noble). Create a virtual package to
	# satisfy the dependency on newer Ubuntu releases.
	if [[ "$RELEASE" == "noble" || "$RELEASE" == "oracular" || "$RELEASE" == "plucky" ]]; then
		chroot_sdcard_apt_get_install equivs
		cat > "${SDCARD}/tmp/libgcc1.control" << 'CONTROL'
Package: libgcc1
Version: 1:999
Architecture: arm64
Maintainer: Armbian <info@armbian.com>
Description: Virtual package satisfying libgcc1 dependency (replaced by libgcc-s1)
Depends: libgcc-s1
CONTROL
		chroot_sdcard "cd /tmp && equivs-build libgcc1.control && dpkg -i libgcc1_*.deb"
		rm -f "${SDCARD}/tmp/libgcc1.control" "${SDCARD}/tmp/libgcc1_"*.deb
	fi

	# Download and install with dpkg --force-depends to bypass apt resolver
	# issues (libgcc1 already handled above, but apt may still refuse due
	# to other outdated dependency declarations in the Seeed .deb).
	local libmali_deb_url="https://seeed-studio.github.io/seeed_armbian_extension/pool/main/libm/libmali/libmali-bifrost-g52-g24p0-x11-wayland-gbm_1.9-1_arm64.deb"
	local libmali_deb="${SDCARD}/tmp/libmali-g52.deb"
	curl -fsSL --connect-timeout 30 --retry 3 -o "${libmali_deb}" "${libmali_deb_url}"
	chroot_sdcard "dpkg -i --force-depends /tmp/libmali-g52.deb"
	chroot_sdcard "apt-get -y -qq -o Dpkg::Use-Pty=0 -f install || true"
	rm -f "${libmali_deb}"
	chroot_sdcard ldconfig

	display_alert "Mali GPU libraries installed successfully" "libmali-g52" "info"
}

# Install camera engine rkaiq for Rockchip ISP
function pre_install_distribution_specific__recomputer_rk3576_install_camera_engine() {
	display_alert "Installing camera engine from APT repo" "camera-engine-rkaiq" "info"

	seeed_recomputer_install_from_apt camera-engine-rkaiq-rk3576

	display_alert "Camera engine installed successfully" "camera-engine-rkaiq" "info"
}

# Audio naming: HDMI0, DP0, ES8311
function post_family_tweaks__recomputer_rk3576_naming_audios() {
	display_alert "$BOARD" "Renaming recomputer-rk3576-module audios" "info"

	mkdir -p $SDCARD/etc/udev/rules.d/
	echo 'SUBSYSTEM=="sound", ENV{ID_PATH}=="platform-hdmi0-sound", ENV{SOUND_DESCRIPTION}="HDMI0 Audio"' > $SDCARD/etc/udev/rules.d/90-naming-audios.rules
	echo 'SUBSYSTEM=="sound", ENV{ID_PATH}=="platform-dp0-sound", ENV{SOUND_DESCRIPTION}="DP0 Audio"' >> $SDCARD/etc/udev/rules.d/90-naming-audios.rules
	echo 'SUBSYSTEM=="sound", ENV{ID_PATH}=="platform-es8311-sound", ENV{SOUND_DESCRIPTION}="ES8311 Audio"' >> $SDCARD/etc/udev/rules.d/90-naming-audios.rules
}

# DMA heap permissions — Mali GBM needs access to /dev/dma_heap/* devices.
# Without this, Mali gbm_create_device fails with ENOENT for non-root users.
function post_family_tweaks__recomputer_rk3576_dma_heap_permissions() {
	display_alert "$BOARD" "Setting DMA heap permissions for Mali GPU" "info"

	mkdir -p $SDCARD/etc/udev/rules.d/
	echo 'SUBSYSTEM=="dma_heap", MODE="0666"' > $SDCARD/etc/udev/rules.d/50-mali-dma-heap.rules
}

# X11 KMS config — rk3576 display-subsystem is /dev/dri/card0
function post_family_tweaks__recomputer_rk3576_x11_kms_config() {
	display_alert "$BOARD" "Configuring X11 for Rockchip KMS" "info"

	mkdir -p $SDCARD/etc/X11/xorg.conf.d/
	cat > $SDCARD/etc/X11/xorg.conf.d/10-rockchip-kms.conf <<-'EOF'
Section "Device"
    Identifier  "Rockchip-KMS"
    Driver      "modesetting"
    Option      "kmsdev" "/dev/dri/card0"
    Option      "PrimaryGPU" "true"
EndSection
EOF
}

# Disable suspend and hibernation
function post_family_tweaks__recomputer_rk3576_disable_suspend() {
	display_alert "$BOARD" "Disabling suspend and hibernation" "info"

	mkdir -p $SDCARD/etc/systemd/sleep.conf.d
	cat > $SDCARD/etc/systemd/sleep.conf.d/nosuspend.conf <<-'EOF'
[Sleep]
AllowSuspend=no
AllowHibernation=no
AllowSuspendThenHibernate=no
AllowHybridSleep=no
EOF
}
